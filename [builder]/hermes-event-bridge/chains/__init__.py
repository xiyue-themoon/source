"""User chain plugins for hermes-event-bridge.

Each module exposes `plugin` (a protocol.ChainPlugin instance). The bridge
auto-discovers them at registration time. Files starting with _ are ignored.
"""
